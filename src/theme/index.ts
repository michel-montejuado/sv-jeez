import { createTheme } from "@mui/material/styles";

export default createTheme({
  colorSchemes: {
    light: {
      palette: {
        primary: {
          main: "#2300beff"
        },
        secondary: {
          main: "#ff00c8ff"
        }
      }
    },
    dark: {
      palette: {
        primary: {
          main: "#2300beff"
        },
        secondary: {
          main: "#ff00c8ff"
        }
      }
    }
  }
});